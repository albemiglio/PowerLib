package it.mycraft.powerlib.common.objects.enums;

/**
 * A site that may host a plugin resource or release.
 */
public enum SiteType {

    /** SpigotMC resource page. */
    SPIGOTMC,
    /** GitHub repository or release. */
    GITHUB,
    /** Any other site. */
    OTHER
}
